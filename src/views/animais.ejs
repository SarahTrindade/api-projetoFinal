<h1>Animais</h1>


<% if (flash) { %>

    <p>
        <%= flash %>
    </p>

<% } %>



<% animais.forEach(animal => { %>

<div>

    <p><%= animal.nome %></p>

    <% if(animal.foto){ %>

        <img src="<%= animal.foto %>" width="150">

    <% } %>

</div>


<% }) %>